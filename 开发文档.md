# AI Enterprise Workspace 开发文档

> 2026-09-14：本文件保留为历史架构方案。后续开发以《AI Enterprise Workspace 项目描述.md》为产品目标，以 [重开发路线](docs/重开发路线.md) 为阶段与验收基线；下文原型进度不能视为新版已完成能力。

版本：v0.1（架构基线）  
日期：2026-09-13  
状态：可进入 MVP 开发

## 1. 项目目标

构建一个 Tencent-style AI Enterprise Workspace（企业级多智能体智能工作台）。用户只需描述目标，例如“整理本周项目进展并生成周报”，系统由 Supervisor 动态拆解任务，调度多个专业 Agent，调用企业工具和知识库，经过审核后输出结果或执行操作。

首个可演示闭环：

```text
用户目标 → Supervisor 规划 → 项目/知识/数据 Agent 并行执行
→ Writer 生成周报 → Reviewer 审核 → 用户确认 → 导出/发送
```

## 2. 候选多智能体框架与决策

| 框架 | 优势 | 局限 | 与本项目匹配度 |
|---|---|---|---|
| MetaGPT | 软件研发流程、角色和 SOP 完整 | 偏代码生产，定制业务工作流成本高 | 中 |
| ChatDev | 多角色对话和协作演示直观 | 生产级状态、工具治理和业务集成较弱 | 中低 |
| CrewAI | Role/Goal/Task 抽象清晰，顺序与并行流程易用，工具封装简单 | 复杂状态图和长流程需自行补充运行时 | **高** |
| AutoGen | 对话式协作灵活，适合辩论、协商和动态群聊 | 行为不够确定，生产编排、审计和成本控制需要较多工程化 | 中高 |

### 2.1 选型结论：CrewAI

MVP 采用 **CrewAI 作为 Python Agent Runtime**，Java 服务负责业务域、权限、任务持久化和消息总线。选择理由：

1. Agent 定义天然对应 AI 管家、数据分析师、文档专家、会议秘书等 AI 员工。
2. Task/Crew/Process 能快速实现顺序、并行和条件分支，便于先做出可演示闭环。
3. 工具可封装为统一 Tool Contract，后续接 MCP、企业微信、CRM、数据库时无需改业务模型。
4. 便于控制 Agent 白名单、超时、重试、Token 预算和人工确认。

不在 MVP 同时引入其他框架。若后续需要多轮辩论或复杂协商，再评估在独立能力模块中引入 AutoGen；框架替换必须通过统一 Agent Runtime 接口完成。

## 3. 总体架构

```text
React + TypeScript Workspace
          ↓ WebSocket/SSE
Spring Cloud Gateway
          ↓
Task / Agent / Knowledge / Document / Message Services
          ↓ RabbitMQ（任务与事件）
Python Agent Runtime（CrewAI + Planner + Tool Adapter）
          ├─ LLM Gateway
          ├─ RAG Retriever
          └─ MCP/企业工具

MySQL：业务数据与 Agent 定义
Redis：任务状态、幂等、短期上下文
Elasticsearch：全文检索与文档索引
MinIO：原始文件与生成物
```

### 3.1 服务边界

- **Gateway**：认证、限流、路由、SSE/WebSocket 转发。
- **Task Service**：任务生命周期、执行计划、审批和审计。
- **Agent Service**：AI 员工定义、版本、能力和工具授权。
- **Knowledge Service**：文档入库、切分、向量检索、引用来源。
- **Document Service**：PDF/Word/Excel/PPT 解析与生成。
- **Message Service**：消息入口、模拟企业微信适配器、发送确认。
- **Agent Runtime**：CrewAI 执行、工具调用、结果标准化、重试和回传。

## 4. 多智能体运行模型

### 4.1 Agent 角色

MVP 实现：Supervisor、Project、Knowledge、Data Analyst、Document/Writer、Reviewer、Communication。HR、CRM、Meeting 在第二阶段加入。

每个 Agent 必须声明：`name`、`role`、`goal`、`backstory`、`allowed_tools`、`knowledge_scopes`、`max_iterations`、`timeout_seconds`。

### 4.2 结构化任务与结果

任务输入和 Agent 输出统一使用 JSON：

```json
{
  "taskId": "T1001",
  "agent": "project_agent",
  "status": "SUCCESS",
  "data": {"facts": [], "metrics": [], "artifacts": []},
  "citations": [],
  "confidence": 0.92,
  "nextAction": "WRITE_REPORT",
  "error": null
}
```

禁止 Agent 之间只传自由文本；所有结果必须可审计、可重试、可被下游校验。

### 4.3 编排策略

- Supervisor 先生成受 schema 约束的 Plan JSON。
- 无依赖任务并行执行；有依赖任务按 DAG 顺序执行。
- 每个节点设置超时和最多 2 次重试，使用幂等键 `taskId + nodeId + attempt`。
- Reviewer 返回 `PASS` 或结构化反馈；`FAIL` 时仅重跑受影响节点，最多 2 轮。
- 发送消息、创建任务、写入 CRM 等副作用操作必须进入 `PENDING_CONFIRMATION`，由用户确认后执行。

## 5. 核心接口

### 5.1 创建任务

`POST /v1/tasks`

```json
{"prompt":"整理本周项目进展并生成周报","workspaceId":"W1"}
```

返回：`taskId`、`status`、`plan`。

### 5.2 查询任务与事件

- `GET /v1/tasks/{taskId}`
- `GET /v1/tasks/{taskId}/events`
- `GET /v1/tasks/{taskId}/stream`（SSE）
- `GET /v1/tasks/{taskId}/export`（Markdown 结果导出）
- `POST /v1/tasks/{taskId}/confirm`
- `POST /v1/tasks/{taskId}/cancel`

### 5.3 Agent Runtime 消息

RabbitMQ exchange：`agent.events`

- `agent.task.requested`
- `agent.task.started`
- `agent.task.result`
- `agent.task.failed`
- `agent.approval.required`

## 6. 数据模型（MVP）

- `workspace`：企业空间与成员。
- `agent_definition`：Agent 基本信息、提示词、版本、状态。
- `agent_tool_binding`：Agent 与工具的授权关系。
- `task`：用户目标、状态、当前节点、重试次数。
- `task_node`：计划节点、依赖、输入输出、耗时和 Token 用量。
- `task_event`：事件流水和审计日志。
- `knowledge_document` / `knowledge_chunk`：文档元数据与切片。
- `artifact`：报告、文件、图表等生成物。

## 7. 安全与治理

采用 JWT + RBAC；工具按 Agent 和 workspace 双重授权。数据库查询只允许只读账号和参数化 SQL。文件下载使用短时签名 URL。提示词、工具参数、模型调用和人工审批全部写入审计日志；敏感字段在日志中脱敏。设置单任务 Token、时长和费用上限。

## 8. 前端页面

Dashboard、AI Assistant、AI Employees、Tasks、Knowledge Base、Documents、Reports、Messages、Admin。任务详情页必须展示计划 DAG、节点状态、来源引用、重试记录和待确认操作。

## 9. 开发阶段与验收

### Phase 0（1 周）基础工程

仓库、Docker Compose、Gateway、MySQL/Redis/RabbitMQ/MinIO、统一鉴权和日志。验收：本地一键启动，服务健康检查通过。

### Phase 1（2 周）Agent Runtime

CrewAI Supervisor、Project、Knowledge、Writer、Reviewer；结构化结果协议；RabbitMQ Worker。验收：固定示例可完成“项目资料→周报→审核”。

### Phase 2（2 周）工作台闭环

任务中心、SSE 进度、文件上传、RAG 引用、报告导出、人工确认。验收：用户可从一句话进入完整闭环。

### Phase 3（2 周）并行与可靠性

DAG 计划、并行节点、重试、幂等、取消、超时和审计。验收：并行任务耗时下降，故障节点可恢复且不重复执行副作用。

### Phase 4（2–3 周）业务扩展

Meeting、CRM、Communication 适配器，定时任务和模拟企业微信入口。验收：会议总结建任务、日报定时生成、消息发送需确认。

## 10. 验证指标

- 计划生成成功率 ≥ 95%
- 结构化结果解析成功率 ≥ 99%
- 可重试失败任务恢复率 ≥ 90%
- 知识库回答均带来源
- 副作用工具 100% 经过授权和确认
- 关键链路具备 traceId，可从前端追溯到 Agent 调用

## 11. 首个 Demo 脚本

上传三份项目周报和一份会议纪要，输入“整理本周项目进展并生成管理层周报”。展示 Supervisor 规划、Project/Knowledge 并行、Writer 输出、Reviewer 反馈、重新生成、用户确认导出 PDF；最后展示任务事件和审计记录。

## 12. 已确认的技术参数

### 12.1 LLM：阿里云百炼业务空间 OpenAI 兼容接口

默认业务空间来自项目内的 `默认业务空间-apiKey-7215423.csv`。运行时使用 OpenAI 兼容客户端，配置如下：

- `LLM_BASE_URL=https://ws-fhr1u2m9mysymhf3.cn-beijing.maas.aliyuncs.com/compatible-mode/v1`
- `LLM_API_KEY`：从本地 `.env` 注入，不写入代码、文档或镜像
- `LLM_MODEL`：默认 `qwen3.7-flash`；当前业务空间确认可用 `qwen3.7-flash`、`kimi-k3`、`qwen3.8-27b`，可按成本、延迟和质量在 `.env` 中切换
- `LLM_WORKSPACE_ID=ws-fhr1u2m9mysymhf3`

CrewAI、Embedding 和 Rerank 调用统一经过 `LLM Gateway`，业务 Agent 不直接读取 API Key。建议生产环境使用 Docker secrets 或外部密钥管理；该 CSV 中的密钥属于敏感凭证，部署前应确认其仅保存在本地并按需轮换。

示例 `.env`（仅保存在本机，加入 `.gitignore`）：

```dotenv
LLM_BASE_URL=https://ws-fhr1u2m9mysymhf3.cn-beijing.maas.aliyuncs.com/compatible-mode/v1
LLM_API_KEY=<从 CSV 注入>
LLM_MODEL=<业务空间中的模型名称>
LLM_WORKSPACE_ID=ws-fhr1u2m9mysymhf3
```

### 12.2 向量检索：Elasticsearch

Elasticsearch 同时承担全文检索、向量检索和引用过滤。MVP 使用 `dense_vector` 字段保存 Embedding，按 `workspaceId`、文档权限和 `knowledgeScope` 过滤后进行 kNN 检索；检索结果必须返回文档 ID、片段 ID、标题、页码或段落位置，供回答引用。

当前索引：`knowledge_chunks_v3`。核心字段：`chunkId`、`workspaceId`、`documentId`、`title`、`content`、`embedding`、`metadata`、`createdAt`。Embedding 模型为 `qwen3.7-text-embedding-flash`，维度 1024；索引 mapping 使用 Elasticsearch `dense_vector`（cosine），查询优先使用 kNN，失败时回退全文检索。

### 12.3 部署：Docker Compose

开发和 MVP 环境采用 Docker Compose，至少包含：`gateway`、`task-service`、`agent-service`、`knowledge-service`、`document-service`、`message-service`、`agent-runtime`、`mysql`、`redis`、`rabbitmq`、`elasticsearch`、`minio` 和前端服务。

要求：

1. 所有服务提供 `/health` 或等价健康检查，使用 `depends_on` 的健康条件控制启动顺序。
2. MySQL、RabbitMQ、Elasticsearch、MinIO 使用命名卷持久化。
3. 配置通过 `.env` 和 Compose secrets 注入，镜像中不得包含 API Key。
4. 为 Elasticsearch 预设内存、磁盘和单节点开发模式；生产环境再拆分为集群部署。
5. 前端通过 Gateway 访问后端，Agent Runtime 不直接暴露公网端口。

推荐文件结构：

```text
deploy/docker-compose.yml
deploy/.env.example
deploy/elasticsearch/init/
services/agent-runtime/Dockerfile
```

这些确认项不会改变 Agent 业务接口，后续更换模型或部署平台只需替换基础设施适配层。

## 13. 当前可运行实现

当前仓库已提供 `services/agent-runtime` 可运行 MVP：FastAPI 接口、CrewAI Flow 计划编排、CrewAI Supervisor LLM 调用、SQLite 任务状态、SSE 事件流，以及 Elasticsearch 文档分块入库和 kNN 检索。Compose 服务包含 Agent Runtime、Agent Worker、Elasticsearch 和 RabbitMQ。CrewAI 返回的计划会经过 JSON 提取、字段归一化和 DAG 依赖校验；解析失败时自动回退到确定性计划。任务结果读取和保存时会过滤 `embedding` 字段，避免把向量传给前端或继续写入任务记录；已完成任务可通过 `/v1/tasks/{task_id}/export` 下载 Markdown。

工作台已增加基础 Workspace 导航和两个产品目录接口：`GET /v1/dashboard?workspace_id=default` 汇总任务状态、知识库文档/分块数及员工可用数；`GET /v1/employees` 返回九个预置 AI 员工的职责、能力和可用状态。前端 Dashboard、AI 员工、任务中心和知识库视图复用同一运行时，窄屏下导航自动变为可横向滚动的导航条。Elasticsearch 暂不可用时 Dashboard 保留任务统计并返回 `knowledge.available=false`。

任务状态会经历 `PENDING → RUNNING → SUCCESS/FAILED`，副作用任务先进入 `PENDING_CONFIRMATION`。API 将任务投递到 RabbitMQ，Worker 按任务 ID 消费；失败任务最多按 `MAX_TASK_RETRIES` 重试。运行时写入节点开始/完成、尝试和最终状态事件；超过 `MAX_TASK_SECONDS` 会以失败结果落库。确认和取消接口分别为 `POST /v1/tasks/{task_id}/confirm` 与 `POST /v1/tasks/{task_id}/cancel`。

节点执行已覆盖 Project、Knowledge、Writer、Reviewer 四类 Agent：Knowledge 节点返回 Elasticsearch 引用，Writer 聚合输入并输出结构化报告，Reviewer 返回 `PASS/FAIL`；Reviewer 失败会写入 `review.feedback` 并触发最多两轮 Writer 修订。

### 13.1 本地启动与验收

```powershell
docker compose --env-file deploy/.env -f deploy/docker-compose.yml up --build -d
Invoke-RestMethod http://localhost:8000/health
Invoke-RestMethod -Method Post http://localhost:8000/v1/tasks -ContentType 'application/json' -Body '{"prompt":"整理本周项目进展并生成周报","workspace_id":"default"}'
```

写入知识文档后，等待索引刷新再调用 `/v1/knowledge/search`；接口优先执行 Elasticsearch kNN，向量查询失败时回退到 `multi_match` 全文检索。
